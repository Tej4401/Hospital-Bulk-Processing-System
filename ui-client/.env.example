# Backend URL for nginx to proxy /api requests to.
# For Docker Compose, use the backend service host or host gateway.
# Examples:
#   BACKEND_URL=http://web:8080
#   BACKEND_URL=https://web:8443
#   BACKEND_URL=http://host.docker.internal:8080
BACKEND_URL=http://host.docker.internal:8080
